
#include <iostream>
#include <iomanip>
#include <vector>
#include <string>
#include <fstream>
#include <cstdlib>
using namespace std;

int Randam_Number_In_Range(int From, int To)
{

	int RandNum = rand() % (To - From + 1) + From;

	return RandNum;
}

void Fill_Matrix_W_Randum_Numbers(int Matrix[3][3], const short& Rows, const short& Coloms)
{

	for (int i = 0; i < Rows; i++)
	{

		for (int j = 0; j < Coloms; j++)
		{
			Matrix[i][j] = Randam_Number_In_Range(1, 10);
		}

	}


}

void Print_Two_Dimensional_Arrays(int Matrix[3][3], const short& Rows, const short& Coloms)
{

	for (int i = 0; i < Rows; i++)
	{

		for (int j = 0; j < Coloms; j++)
		{
			printf("%02d\t", Matrix[i][j]);
			//cout << setw(3) << Arr[i][j] << "    ";
		}

		cout << endl;

	}


}

void Multiply_two_Matrixs(int Matrix_1[3][3], int Matrix_2[3][3], int Multiply_Matrix[3][3], const short& Rows, const short& Coloms)
{

	for (int i = 0; i < Rows; i++)
	{
		for (int j = 0; j < Coloms; j++)
		{

			Multiply_Matrix[i][j] = Matrix_1[i][j] * Matrix_2[i][j];

		}
	}

}

int main()
{
	srand((unsigned)time(NULL));

	int Matrix_1[3][3], Matrix_2[3][3], Multiply_Matrix[3][3];


	Fill_Matrix_W_Randum_Numbers(Matrix_1, 3, 3);
	cout << "Matrex 1 : \n\n";
	Print_Two_Dimensional_Arrays(Matrix_1, 3, 3);
	cout << "\n\n" << endl;

	Fill_Matrix_W_Randum_Numbers(Matrix_2, 3, 3);
	cout << "Matrex 2 : \n\n";
	Print_Two_Dimensional_Arrays(Matrix_2, 3, 3);
	cout << "\n\n" << endl;

	cout << "The Result : \n\n";
	Multiply_two_Matrixs(Matrix_1, Matrix_2, Multiply_Matrix, 3, 3);
	Print_Two_Dimensional_Arrays(Multiply_Matrix, 3, 3);
	cout << "\n\n" << endl;



	return 0;
}