
#include <iostream>
#include <iomanip>
#include <vector>
#include <string>
#include <fstream>
#include <cstdlib>
using namespace std;

int Randam_Number_In_Range(int From, int To)
{

	int RandNum = rand() % (To - From + 1) + From;

	return RandNum;
}

void Fill_Two_Dimensional_Arrays(int Arr[3][3], const short & Rows , const short & Coloms)
{

	for (int i = 0; i < Rows; i++)
	{

		for (int j = 0; j < Coloms; j++)
		{
			Arr[i][j] = Randam_Number_In_Range(1,100);
		}

	}


}

int Sum_Row_of_Matrix(int Arr[3][3], const short& Row_Number, const short& Coloms)
{

	int sum = 0;
	for (int j = 0; j < Coloms; j++)
	{
		sum += Arr[Row_Number][j];
	}

	return sum;
}

void Print_Rows_sum_of_Matrix(int Arr[3][3], const short& Rows, const short& Coloms)
{

	for (int i = 0; i < Rows; i++)
	{
		cout << "Sum of Raw [" << i + 1 << "] is : " << Sum_Row_of_Matrix(Arr, i, 3) << "\n";
	}

}

void Print_Two_Dimensional_Arrays(int Arr[3][3], const short& Rows, const short& Coloms)
{

	for (int i = 0; i < Rows; i++)
	{

		for (int j = 0; j < Coloms; j++)
		{
			//printf("%d\t", Arr[i][j]);
			cout << setw(3) << Arr[i][j] << "    ";
		}

		cout << endl;

	}


}


int main()
{
	srand((unsigned)time(NULL));

	int arr[3][3];
	Fill_Two_Dimensional_Arrays(arr,3,3);
	cout << "The 3x3 array result : \n\n";
	Print_Two_Dimensional_Arrays(arr,3,3);
	cout << "\n\n" << endl;
	Print_Rows_sum_of_Matrix(arr, 3, 3);

	cout << "\n\n" << endl;



	return 0;
}