#include <iostream>
#include <cstdlib>
using namespace std;


int Enter_Positive_Number(string Massege)
{

	int Number = 0;

	do
	{

		cout << Massege;
		cin >> Number;
		cout << endl;


	} while (Number <= 0);


	return Number;
}

int Randam_Number_In_Range(int From, int To)
{
	int RandNum = rand() % (To - From + 1) + From;
	return RandNum;
}

void Fill_N_Array_with_Randum_Number(int Arr[100], int& ArrLength)
{

	ArrLength = Enter_Positive_Number("\nPlease enter a Number : ");


	for (int i = 0; i < ArrLength; i++)
	{

		Arr[i] = Randam_Number_In_Range(1, 100);

	}

}

void Print_Array(int Arr[100], int ArrLength)
{


	for (int i = 0; i < ArrLength; i++)
	{

		cout << Arr[i] << " ";

	}

	cout << endl;


}

void Copy_Array(int Arr_Origin[100], int Arr_copy[100], int ArrLength)
{


	for (int i = 0; i < ArrLength; i++)
	{

		Arr_copy[i] = Arr_Origin[i];

	}



}



short Number_Position(int Arr[100], int ArrLength, int Num_to_Search)
{

	
	for (int i = 0; i < ArrLength; i++)
	{
		if (Arr[i] == Num_to_Search)
		{

			return i;

		}

	}

		return -1;

}


int main()
{

	srand((unsigned)time(NULL));

	int Arr[100], ArrLength = 0;

	Fill_N_Array_with_Randum_Number(Arr, ArrLength);


	cout << "Array 1 Elements             : ";
	Print_Array(Arr, ArrLength);


	int Num_to_Search = Enter_Positive_Number("\nEnter Number to Search for?  : ");

	short Num_Position_in_Arr = Number_Position(Arr, ArrLength, Num_to_Search);


	cout << "The Number you search   for  : " << Num_to_Search << endl << endl;

	if (Num_Position_in_Arr == -1)
	{
		cout << "Number You seache for Not found  :-( \n";
	}
	else
	{
		cout << "Number found at Position     : " << Num_Position_in_Arr << endl;
		cout << "\nNumber     Order     is      : " << Num_Position_in_Arr + 1 << endl;

	}



	return 0;
}