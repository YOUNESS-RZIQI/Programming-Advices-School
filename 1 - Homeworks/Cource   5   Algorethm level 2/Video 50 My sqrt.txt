#include <iostream>
#include <cmath>
using namespace std;


float Read_Number()
{
	float Num = 0;

	cout << "Please enter The Number : ";
	cin >> Num;

	return Num;
}

float Get_Fraction(float Number)
{
	return Number - int(Number);
}

float My_sqrt(float Number)
{
	return pow(Number , 1.0/2.0); // or pow(Number , 0.t) it will gave you sprt because this is the rule
}


int main()
{

	float Number = Read_Number();

	cout << "\nMy sqrt  : " << My_sqrt(Number);
	cout << "\nC++ sqrt : " << sqrt(Number) << endl;

	return 0;
}