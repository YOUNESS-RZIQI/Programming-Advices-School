
#include <iostream>
#include <iomanip>
#include <vector>
#include <string>
#include <fstream>
#include <cstdlib>
#include <cmath>
using namespace std;

int Randam_Number_In_Range(int From, int To)
{

	int RandNum = rand() % (To - From + 1) + From;

	return RandNum;
}

void Fill_Matrix_W_Randum_Numbers(int Arr[3][3], const short& Rows, const short& Coloms)
{

	for (int i = 0; i < Rows; i++)
	{

		for (int j = 0; j < Coloms; j++)
		{
			Arr[i][j] = Randam_Number_In_Range(1, 10);
		}

	}


}

void Print_Matrix(int Arr[3][3], const short& Rows, const short& Coloms)
{

	for (int i = 0; i < Rows; i++)
	{

		for (int j = 0; j < Coloms; j++)
		{
			printf("%02d\t", Arr[i][j]);
			//cout << setw(3) << Arr[i][j] << "    ";
		}

		cout << endl;

	}


}

void Print_Medl_Row_of_Matrix(int Arr[3][3], const short& Rows, const short& Coloms)
{

	int Medl_Row = Rows / 2;

		for (int j = 0; j < Coloms; j++)
		{
			printf("%02d\t", Arr[Medl_Row][j]);
			//cout << setw(3) << Arr[i][j] << "    ";
		}

		cout << endl;

}

void Print_Medl_Colom_of_Matrix(int Arr[3][3], const short& Rows, const short& Coloms)
{

	int Medl_Colm = Coloms / 2;

	for (int j = 0; j < Rows; j++)
	{
		printf("%02d\t", Arr[j][Medl_Colm]);
		//cout << setw(3) << Arr[i][j] << "    ";
	}

	cout << endl;

}

int main()
{
	srand((unsigned)time(NULL));

	int arr[3][3];
	Fill_Matrix_W_Randum_Numbers(arr, 3, 3);
	cout << "The Matrex : \n\n";
	Print_Matrix(arr, 3, 3); 
	cout << "\n\n" << endl;

	cout << "The Medel Row Line : \n\n";

	Print_Medl_Row_of_Matrix(arr, 3, 3);

	cout << "\n\n" << endl;

	cout << "The Medel Colom : \n\n";

	Print_Medl_Colom_of_Matrix(arr, 3, 3);

	cout << "\n\n" << endl;



	return 0;
}